# RFP Copilot v2 — zero-cost stack. No paid API dependency.
# Python 3.11+

# --- Configuration ---
pydantic>=2.7
pydantic-settings>=2.3

# --- LLM providers ---
# Generation runs on free-tier cloud; Ollama is the offline degradation path.
# All three cloud SDKs are optional at import time — provider.py degrades if absent.
httpx>=0.27
groq>=0.11
google-genai>=0.3
huggingface-hub>=0.24

# --- Embeddings & reranking (local — deliberately not cloud, see CLAUDE.md) ---
sentence-transformers>=3.0
torch>=2.2

# --- Retrieval ---
chromadb>=0.5
rank-bm25>=0.2.2

# --- Document parsing ---
python-docx>=1.1
pypdf>=4.2
openpyxl>=3.1
markdown>=3.6

# --- Charts (deterministic, no LLM) ---
matplotlib>=3.8

# --- UI ---
streamlit>=1.35

# --- Testing ---
pytest>=8.2
